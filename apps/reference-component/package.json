{
  "name": "reference-component",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "definition:extract": "pnpm exec ts-node scripts/extract-definition.ts",
    "build": "tsc -p tsconfig.json && pnpm run definition:extract"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.0",
    "@qubbi/contract": "workspace:*",
    "@qubbi/openapi": "workspace:*"
  }
}
