{
  "name": "reference-component",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "extract-definition": "tsx scripts/extract-definition.ts",
    "build": "tsc -p tsconfig.json && pnpm run extract-definition"
  },
  "dependencies": {
    "@qubbi/contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  }
}
